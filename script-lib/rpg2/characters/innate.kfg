
[[doctype spellcast/book]]

[item-model fists]
	class: innate
	label: fists
	slotType: hand
	slotCount: 2
	usages:
		melee-fighting:
			primary:
				params:
					label: punch
					criticalHitFlavor: $$> ^r${performer.label//uc1} punches ${target.label} for ${damages} hp.
					fatalHitFlavor: $$> ^r${performer.label//uc1} thumps ${target.label} for ${damages} hp.
				# No bonus, this is the default/standard/normal attack

[item-model fangs]
	class: innate
	label: fangs
	slotType: innate
	slotCount: 1
	usages:
		melee-fighting:
			primary:
				params:
					criticalHitFlavor: $$> ^r${performer.label//uc1} bites ${target.label} for ${damages} hp.
					fatalHitFlavor: $$> ^r${performer.label//uc1} shreds ${target.label} for ${damages} hp.
				compound:
					damages: (+) 4

