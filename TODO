


== ADVENTURER MODE ==

Bugs:
- when a client reconnect, it should not trigger a roleList event again for other players (it freezes clients)

Tags:
- aside
- action
- quest (a kind of sub-scene that can be resumed, like a *generator* in programming language)
- entry / widget / item

Multiplayer tags:
- chat
- profile
- next tag:
	- on-select
	- deselectable (deselect before others have done)

Browser UI event implementation:
- sound
- music



== SPELLCASTER MODE (a.k.a. TASK RUNNER) ==

- be compatible with Gulp plugins?
- group dependencies into a [dependencies] tag?

