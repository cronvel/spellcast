
[[doctype spellcaster]]

[formula]
	list:
		- one
		- two
		- three

[spell foreach]
	[foreach $list => $value]
		[message]
			$> The value is: ${value}
	
	[foreach $list => $key : $value]
		[message]
			$> The key/value is: ${key}/${value}

